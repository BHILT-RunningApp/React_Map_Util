import React, { Component } from "react";

import { MapLayer } from "react-leaflet";
import L from "leaflet";
import "leaflet-routing-machine";

class LeafletRoutingMachine extends Component {
  render() {
    return <div></div>;
  }
}

export default LeafletRoutingMachine;
